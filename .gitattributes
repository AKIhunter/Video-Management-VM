# 统一换行符，避免跨平台协作时产生整文件 diff
* text=auto

# 脚本按各自平台应有的换行符存储（关键：run.sh 必须是 LF，否则 Linux 下无法执行）
*.sh   text eol=lf
*.py   text eol=lf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# 二进制文件不做换行转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.ico  binary
*.svg  text eol=lf
